@page "/admin/modules"
@attribute [Microsoft.AspNetCore.Authorization.Authorize(Roles = "admin")]
@inject NavigationManager Nav

@code {
    protected override void OnInitialized() =>
        Nav.NavigateTo("/admin/system-config?tab=modules", replace: true);
}
